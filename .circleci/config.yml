version: 2
jobs:
  build:
    working_directory: ~/paymentservice
    docker:
      - image: 'circleci/openjdk:11-stretch'
    steps:
      - add_ssh_keys:
          fingerprints:
            - 'e6:00:71:ec:7c:cd:25:43:12:37:65:38:59:51:d4:e3'
      - checkout
      - restore_cache:
          key: 'paymentservice-{{ checksum "pom.xml" }}'
      - run: 'mvn dependency:go-offline'
      - save_cache:
          paths:
            - ~/.m2
          key: 'paymentservice-{{ checksum "pom.xml" }}'
      - run: mvn package
      - store_test_results:
          path: target/surefire-reports
      - store_artifacts:
          path: target/paymentservice.jar
      - store_artifacts:
          path: /tmp/artifact-1
          destination: artifact-file
      - store_artifacts:
          path: /tmp/artifacts
  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: Deploy Over SSH
          command: |
            ssh $SSH_USER@$SSH_HOST
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
          filters:
            branches:
              only: master
